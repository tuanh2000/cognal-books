# Local-development overrides (applied automatically by `docker compose`).
# Publishes Postgres to the host so you can run the apps natively
# (`pnpm dev`) against the containerized database.
services:
  postgres:
    ports:
      - '5432:5432'
